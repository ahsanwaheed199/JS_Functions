// lambda fuction refer to the anonymous function.
// mechanism to represent the anonymous function.
// its also called arrow function

let food = (x, y, z) => {
  return x + y + z;
};
console.log(`food ${food(23, 4, 5)}`);

// or;

let food1 = (x) => x;
console.log(`food price : ${food1(34)}`);
